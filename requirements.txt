# Runtime dependencies for the hosted deployment; mirrors pyproject.toml.
# app.py puts src/ on sys.path itself, so the package needs no install here.
# For local development use `pip install -e ".[dev]"` instead.

httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
pandas>=2.2
beautifulsoup4>=4.12
lxml>=5.2
streamlit>=1.36
# Messages API client. Also used against DeepSeek, which is compatible.
anthropic>=0.40
